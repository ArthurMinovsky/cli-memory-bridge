[package]
name = "cli-memory-integrations"
version = "0.2.0"
edition = "2024"

[dependencies]
anyhow.workspace = true
cli-memory-core = { path = "../core" }
rusqlite = { version = "0.32", features = ["bundled"] }
serde_json.workspace = true

[dev-dependencies]
tempfile = "3"
